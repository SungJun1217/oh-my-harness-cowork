{
  "_comment": [
    "Merge this fragment into ~/.claude/settings.json's hooks. Don't overwrite the file.",
    "Install prerequisite: installed via install.sh, or ln -s <repo>/bin/omhc ~/.local/bin/omhc",
    "",
    "Order matters. mark records this session to the ledger first, then brief",
    "reads the counterpart harness's session after it. due() excludes",
    "my_session_id, so a session never hands off to itself.",
    "",
    "Specifies --wire claude explicitly. It doesn't guess the platform from an",
    "env var because omhc is a settings hook, not a plugin, so CLAUDE_PLUGIN_ROOT",
    "is never set. And emitting both additional_context and hookSpecificOutput",
    "at once would have Claude Code read both without deduping, injecting the",
    "handoff twice.",
    "",
    "UserPromptSubmit (v2 phase 2, #42): `omhc turn` warns, once per human",
    "turn, when the other harness modified a file this session already",
    "touched. This is a separate trust/approval unit from SessionStart above —",
    "Claude Code may ask you to approve this new hook group on its own even if",
    "SessionStart is already approved."
  ],
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "$HOME/.local/bin/omhc mark --harness claude-code"
          },
          {
            "type": "command",
            "command": "$HOME/.local/bin/omhc brief --harness claude-code --wire claude"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "$HOME/.local/bin/omhc turn --harness claude-code"
          }
        ]
      }
    ]
  }
}

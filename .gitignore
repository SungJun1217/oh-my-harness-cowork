__pycache__/
*.py[cod]
.claude/settings.local.json
.omhc/

# Fixtures hold real conversation content. This repo has a GitHub remote, so they are
# never committed. Regenerate them on this machine with tests/harvest.py; only the
# golden counts are committed, as tests/fixtures/expected.json.
tests/fixtures/

# Dev sandbox scripts (sandbox/setup.sh, run.sh). Local only.
sandbox/

# pyproject build output
build/
dist/
*.egg-info/

# Claude Code's parallel-agent worktrees. They live inside the repo but are never committed.
.claude/worktrees/

# Zero runtime dependencies, so Actions versions are the only thing to manage. main is
# release-only, so updates target develop.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    target-branch: develop
    schedule:
      interval: monthly
    commit-message:
      prefix: chore(ci)
